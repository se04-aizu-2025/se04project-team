package dotnet.sort.presentation.common.i18n

import dotnet.sort.domain.model.Language

actual fun updateSystemLocale(language: Language) {
    // TODO: Implement browser locale switching if possible
}

#!/bin/bash
# Pre-push hook: プッシュ前にテストとコード品質チェックを実行

echo "🚀 Running pre-push checks..."
echo ""

# ktlintチェック
echo "📝 Running ktlint..."
./gradlew ktlintCheck --daemon
KTLINT_RESULT=$?

if [ $KTLINT_RESULT -ne 0 ]; then
    echo ""
    echo "❌ ktlint check failed"
    echo ""
    echo "💡 Fix suggestions:"
    echo "   1. Auto-fix formatting:"
    echo "      ./gradlew ktlintFormat"
    echo ""
    echo "   2. Then commit:"
    echo "      git add . && git commit -m 'Fix formatting'"
    echo ""
    echo "   3. If you want to push anyway (not recommended):"
    echo "      git push --no-verify"
    echo ""
    exit 1
fi

echo "✅ ktlint passed"
echo ""

# detekt
echo "🔎 Running detekt..."
./gradlew detekt --daemon
DETEKT_RESULT=$?

if [ $DETEKT_RESULT -ne 0 ]; then
    echo ""
    echo "❌ detekt check failed"
    echo ""
    echo "💡 Fix suggestions:"
    echo "   1. View the report:"
    echo "      open build/reports/detekt/detekt.html"
    echo ""
    echo "   2. Fix issues and commit"
    echo ""
    exit 1
fi

echo "✅ detekt passed"
echo ""

# JVMテストのみ実行（JS/WasmJsは遅いのでCIで実行）
echo "🧪 Running JVM tests..."
./gradlew jvmTest --daemon
TEST_RESULT=$?

if [ $TEST_RESULT -ne 0 ]; then
    echo ""
    echo "❌ JVM tests failed"
    echo ""
    echo "💡 Fix suggestions:"
    echo "   1. Run tests locally:"
    echo "      ./gradlew jvmTest --info"
    echo ""
    echo "   2. Full test suite (slow):"
    echo "      ./gradlew allTests"
    echo ""
    echo "   3. Skip tests (not recommended):"
    echo "      git push --no-verify"
    echo ""
    exit 1
fi

echo "✅ JVM tests passed"
echo ""
echo "ℹ️  Note: JS/WasmJs tests will run in CI"
echo ""
echo "========================================="
echo "✅ All pre-push checks passed! Proceeding with push..."
echo "========================================="
exit 0
